name: One-shot Bank receipt delete guard

on:
  workflow_dispatch:
  push:
    branches:
      - feature/bank-classification-phase-a
    paths:
      - '.github/workflows/one-shot-bank-receipt-delete-guard.yml'
      - '.github/scripts/apply_bank_receipt_delete_guard.py'

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/bank-classification-phase-a
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.3.1'
      - name: Create local debug keystore
        run: |
          keytool -genkeypair -v -storetype PKCS12 -keystore "$GITHUB_WORKSPACE/debug.keystore" -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
      - name: Apply receipt delete guard
        run: |
          python .github/scripts/apply_bank_receipt_delete_guard.py
          git diff --check
      - name: Lint
        run: gradle lintDebug
      - name: Unit tests
        run: gradle testDebugUnitTest
      - name: Build debug APK
        run: gradle assembleDebug
      - name: Commit verified receipt delete guard
        run: |
          if git diff --quiet -- app/src/main/java/com/example/ui/ReceiptViewModel.kt; then
            echo "No receipt delete guard diff to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/example/ui/ReceiptViewModel.kt
          git commit -m "Block deleting receipts with active bank links"
          git pull --rebase origin feature/bank-classification-phase-a
          git push origin HEAD:feature/bank-classification-phase-a
