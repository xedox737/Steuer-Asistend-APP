name: Disable automatic index cleanup

on:
  push:
    branches:
      - agent/safe-duplicate-purge
    paths:
      - tools/disable_automatic_index_cleanup.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/safe-duplicate-purge
          fetch-depth: 0
      - name: Apply safe read-only migration
        run: python3 tools/disable_automatic_index_cleanup.py
      - name: Commit repository change
        run: |
          if git diff --quiet; then
            echo "No repository change required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/example/data/DrivePersistenceRepository.kt
          git commit -m "Disable automatic receipt index cleanup"
          git push origin HEAD:agent/safe-duplicate-purge
