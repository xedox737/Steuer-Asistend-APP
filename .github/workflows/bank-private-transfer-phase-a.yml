name: Bank private transfer Phase A

on:
  workflow_dispatch:
  push:
    branches:
      - feature/bank-private-transfer-phase-a-impl2
    paths:
      - .github/workflows/bank-private-transfer-phase-a.yml

permissions:
  contents: write

jobs:
  implement-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/bank-private-transfer-phase-a-impl2
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.3.1"

      - name: Create local debug keystore
        run: |
          if [ ! -f debug.keystore ]; then
            keytool -genkeypair -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -dname "CN=Android Debug,O=Android,C=US" -keyalg RSA -keysize 2048 -validity 10000
          fi

      - name: Apply Bank private transfer Phase A patch
        run: |
          python3 .github/scripts/apply_bank_private_transfer_phase_a.py
          git diff --check

      - name: Lint
        run: gradle lintDebug --stacktrace --no-daemon

      - name: Unit tests
        run: gradle testDebugUnitTest --stacktrace --no-daemon

      - name: Build debug APK
        run: gradle assembleDebug --stacktrace --no-daemon

      - name: Commit verified patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/example/data/BankingModels.kt \
                  app/src/main/java/com/example/data/BankTransactionClassificationPolicy.kt \
                  app/src/main/java/com/example/data/ReceiptDatabase.kt \
                  app/src/main/java/com/example/data/SupplementalDriveBackup.kt \
                  app/src/main/java/com/example/ui/ReceiptViewModel.kt \
                  app/src/main/java/com/example/ui/BankCompactUiPolicy.kt \
                  app/src/main/java/com/example/ui/BankFeature.kt \
                  app/src/test/java/com/example/data/BankPrivateTransferPhaseAAcceptanceTest.kt
          git commit -m "Bank & Belege: Privat und Umbuchung Phase A"
          git push origin HEAD:feature/bank-private-transfer-phase-a-impl2
